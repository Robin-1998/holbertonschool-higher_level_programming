#!/usr/bin/python3
from add_0 import add
if __name__ == "__main__":
    a = 1
    b = 2
print("{} + {} = {}".format(a, b, add(a, b)))
# sert à indiquer que le bloc de code qui suit doit être exécuté uniquement
# si le fichier est exécuté directement, et pas lorsqu'il est importé
# comme module dans un autre fichier.
