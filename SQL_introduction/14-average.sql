-- Script qui calcule la moyenne des scores de tous les enregistrements de la table second_table de la base de données hbtn_0c_0
SELECT AVG(score) AS average
FROM second_table;
-- la moyenne est calculé avec la fonction AVG(colonne)
